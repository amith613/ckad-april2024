apiVersion : rbac.authorization.k8s.io/v1
kind : Role
metadata :
  name : my-role
  
rules :
  - apiGroups : [""] # blank means core API group
    resources : ["pods"]
    verbs : ["get", "list"]


    